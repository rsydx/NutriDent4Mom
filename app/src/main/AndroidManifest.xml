<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nutrident4mom">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main.UpdateGoalActivity"
            android:exported="false"
            android:label="@string/title_activity_update_goal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".main.AddVideoActivity"
            android:exported="false" />
        <activity
            android:name=".main.ReportActivity"
            android:exported="false" />
        <activity
            android:name=".main.AddContentActivity"
            android:exported="false" />
        <activity
            android:name=".main.MessageActivity"
            android:exported="false" />
        <activity
            android:name=".content"
            android:exported="false"
            android:label="@string/title_activity_content"
            android:theme="@style/AppTheme.NoActionBar" /> <!-- <activity android:name=".login.SplashActivity"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity
            android:name=".login.LoginActivity"
            android:exported="true"
            android:label="@string/NutriDent4Mom"
            android:noHistory="false"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main.AdminActivity"
            android:label="admin" />
        <activity
            android:name=".main.MainActivity2"
            android:label="main" />
        <activity
            android:name=".main.GoalCalculator"
            android:label="goal" />
        <activity
            android:name=".main.EditPickedFood"
            android:label="main" />
        <activity
            android:name=".login.RegisterActivity"
            android:label="register" />
        <activity
            android:name=".login.ResetPasswordActivity"
            android:label="reset" /> <!-- <activity -->
        <!-- android:name=".loginFirstTimeUserInfo.InfoActivity_1" -->
        <!-- android:label="info_collection_1" /> -->
        <!-- <activity -->
        <!-- android:name=".loginFirstTimeUserInfo.InfoActivity_2" -->
        <!-- android:label="info_collection_2" /> -->
        <activity android:name=".main.ContentActivity" />
        <activity
            android:name=".main.FoodSearch"
            android:label="foodSearch" />
        <activity
            android:name=".main.PickedFood"
            android:label="foodPick" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" /> <!-- <receiver android:name=".main.AlarmReceiver"/> -->
        <service
            android:name=".main.PushNotificationService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>